" Sources defaults
source $VIMRUNTIME/defaults.vim

" Enables line numbers
set number

" Set tab to 4 columns wide
set tabstop=4 shiftwidth=4 softtabstop=4 smarttab autoindent

" Sets highlighting when searching
set hlsearch
